name: Check for typos

permissions:
  contents: read

on:
  workflow_dispatch:

jobs:
  check-typos:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Run spellcheck
        uses: crate-ci/typos@v1.26.0
